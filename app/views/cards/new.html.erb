<h1>New Card</h1>

<%= form_for @card do |f| %>
  <%= f.hidden_field :slug, value: @card.slug %>
  <%= f.label :title, 'Title for your Card' %>
  <%= f.text_field :title %><br />
  <%= f.submit "Add to Card" %>
  <%= link_to 'Back', cards_path %>
<% end %>
